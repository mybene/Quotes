import { HighligthDirective } from './highligth.directive';

describe('HighligthDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthDirective < any>();
    expect(directive).toBeTruthy();
  });
});
